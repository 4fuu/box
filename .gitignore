# Orb portal registrations. Generated per machine.
.amp/portals/*

# Built binary. `go build -o box .`
/box

# Release binary and license copy staged into the image build context. Not source.
/images/base/box
/images/base/LICENSE
